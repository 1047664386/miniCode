# MiniCodeIDE 逻辑缺口与 Bug 清单

> 本文档系统梳理项目中逻辑未闭环、功能缺失、已知 Bug 和改进方向。
> 按严重程度分级：P0（必须修复）、P1（影响体验）、P2（体验优化）、P3（长期迭代）。
> 
> ✅ = 已修复 / 已实现；🔧 = 正在修复中

---

## 一、Skill 识别与加载系统

### P1: Skill 自动触发识别未实现 ✅

**现状**：已实现基于关键词的 Skill 自动触发机制。

**已实现**：
1. `parseFrontmatter` 升级：支持 `triggers: [keyword1, keyword2]` 数组格式解析
2. `SkillMeta` 扩展：新增 `triggers: string[]` 字段
3. `SkillStore.matchForInput()`：用户输入 → tokenize → 匹配 triggers → 按匹配分排序
4. `renderAutoTriggeredSkills()`：自动触发匹配的 skill 全文注入 context（最多 2 个，每个截断 3000 字）
5. `renderForSystem()` 概览展示 triggers 提示，帮助 LLM 更好判断

**匹配策略**：
- 全串包含匹配：用户输入包含 trigger → 长词(≥3字)权重 2，短词权重 1
- Token 级匹配：输入 token 与 trigger 全词/子串匹配 → 权重 1
- 中文支持：按字 + 二字 bigram 切分

**未实现（P3 远期）**：
- `/skill` slash command 入口
- 超过 30 条 skill 时的 embedding 召回 top-K

### ✅ P2: Skill 热更新 —— 已修复

**修复说明**：`startWatch()` 方法已使用 chokidar 监听 project-level 和 user-level 两个 skills 目录，配置 `ignoreInitial: true, depth: 2`，只监听 `SKILL.md` 文件的 add/change/unlink 事件，触发后自动调用 `this.load()` 全量重新扫描。

**相关代码**：`apps/server/src/skill-store.ts` 第 86-105 行。

### P2: Skill Schema 校验不足

**现状**：frontmatter 解析器是简化的 YAML-lite（只支持 `key: value`），不支持嵌套、数组、多行字符串。

**问题**：如果 skill 定义需要 `triggers: ["deploy", "发布"]` 这样的数组，当前解析器无法处理。

**建议**：引入轻量 YAML 解析（如 `yaml` 库），或至少支持数组语法 `triggers: [a, b]`。

---

## 二、MCP 集成

### ✅ P1: MCP Tool 参数 Schema 缺失 —— 已修复

**修复方案**：新增 `jsonSchemaToZod()` 函数，将 MCP 的 `inputSchema`（JSON Schema）转换为 zod schema。覆盖 object/string/number/boolean/array/enum/nullable 等常见类型。转换失败时 fallback 到 `z.record(z.string(), z.any())`。

**相关代码**：`apps/server/src/mcp-client.ts` 第 269-396 行

### ✅ P2: MCP 进程生命周期管理粗放

**修复方案**：在 `McpClient` 中新增指数退避自动重连机制。MCP server 异常退出（exit code ≠ 0）时，按 1s → 2s → 4s 延迟最多重连 3 次。`close()` 方法会阻止重连。重连结果通过 `onReconnect` 回调通知 McpManager。

**相关代码**：`apps/server/src/mcp-client.ts`

### P3 → P2: MCP 资源（Resources）协议未实现 — ❌ 跳过

**现状**：只实现了 `tools/list` + `tools/call`，没有实现 `resources/list` + `resources/read`。

**跳过理由**：
- 项目已有 `read_file` + `autoContext` 检索机制覆盖 99% 数据注入场景
- Resources 只在 MCP server 提供非文件结构化数据（如数据库行）时有增量价值，当前项目无此需求
- 实现需要改 McpClient + McpManager + ToolRegistry + 前端展示，改动面大但 ROI 极低
- 面试时如有此问题，可回答"ROI 太低，read_file + 索引检索已覆盖"

---

## 三、子 Agent 系统

### ✅ P1: 子 Agent 结果推送时机不可控

**修复方案**：在父 turn 的 finally 块中，除了通过 SSE 推送 `subagent_announce` 事件外，还将 announce 消息作为 user message 注入父 session。同时发送 `subagent_followup` SSE 事件通知前端自动触发 follow-up turn，使父 Agent 能在下一轮处理子 Agent 结果。

**相关代码**：`apps/server/src/main.ts` 第 2231-2254 行

### P2: 子 Agent 并发写冲突

**现状**：多个子 Agent 默认共享同一 workspace，并发写文件可能互相覆盖。`worktree` 隔离是可选的，且 Git worktree 创建有开销。

**问题**：
- 两个子 Agent 同时 `write_file` 同一路径 → 后写的覆盖前写的
- 没有 file lock 机制

**建议**：
1. 默认启用 worktree 隔离（至少对写操作）
2. 增加文件级 advisory lock
3. 子 Agent 完成后 merge worktree 结果到主分支

### P2: 子 Agent 流式输出到前端不完整 ✅

**已实现**：
- 统一 `subagentEventBuffer` 替代原 `subagentProgressBuffer`，容纳 text/tool/progress 三类事件
- `child_text` 事件：按 runId 聚合累积，200ms 节流批量 flush，避免逐 token 发送造成带宽风暴
- `child_tool` 事件：频率低，直接写入 buffer
- `child_tool_result` 事件：沿用原逻辑，写入 buffer
- SSE flush 在 ask 和 agent/plan 两个循环中均已接入，每轮事件消费时先 flush 残留 text 累积再逐条发送
- Buffer 容量上限 300 条，超出丢弃最旧事件

**SSE 事件格式**：
- `{ type: 'subagent_text', runId, text, ts }` — 子 Agent 流式文本（200ms 批量）
- `{ type: 'subagent_tool', runId, tool, ts }` — 子 Agent 工具调用
- `{ type: 'subagent_progress', runId, tool, resultPreview, ts }` — 子 Agent 工具结果

---

## 四、上下文管理

### ✅ P1: Hard Compact 后可能丢失关键推理信息

**修复方案**：在 `summarizeHard()` 中新增决策/推理片段提取。通过中英文决策关键词（"I'll..."、"Let me..."、"我决定..."、"Based on..."等）从 assistant 消息中提取关键推理，保留最多 5 条片段。未匹配到决策关键词时，取 assistant 消息首句作为意图摘要。

**相关代码**：`packages/core/src/context/hard-compact.ts`

### P2: Token 估算精度不足

**现状**：使用启发式公式 `tokens = ceil(cjk/1.35 + other/4) * 1.2`，没有使用真实 tokenizer。

**问题**：
- 对中文内容估算偏保守（多估 20%），导致过早触发压缩
- 对代码片段估算偏差更大（特殊 token 多）

**建议**：
- 接入 `tiktoken`（OpenAI）或 `js-tiktoken` 作为可选注入
- Anthropic 模型使用 Anthropic 自己的 tokenizer 估算

### P3: 没有 Prompt Cache 命中率统计

**现状**：Anthropic Provider 设置了 `cache_control: { type: 'ephemeral' }`，但没有统计缓存命中率和节省的 token 数。

**建议**：在 Anthropic Provider 的 response 中解析 `cache_creation_input_tokens` 和 `cache_read_input_tokens`，上报到 metrics。

---

## 五、记忆系统

### P1 → P2: Auto-Memory 触发条件优化

**现状修正**：经过代码审查，auto-memory 的触发条件已经比文档初版描述的更完善：

**已实现**：
- ✅ 项目知识信号词已添加（"这个项目"、"入口是"、"架构是"、"数据库是"、"部署在"、".env/配置文件"等）
- ✅ `/remember` slash command 已实现（`apps/server/src/slash-commands.ts` 第 124-136 行）
- ✅ 间隔已从 5 降低到 3（`MIN_TURNS_BETWEEN_RUNS = 3`）
- ✅ 自然语言"记住/记一下/别忘了"信号词已添加

**仍可优化**：
- 用户显式 `/remember` 应该绕过信号词和长度限制，直接触发 LLM 提取（当前依赖 slash command expand 让 LLM 调用 upsert_memory，但没有走 auto-memory 的 LLM 提取路径）
- 限流用秒级时间间隔（3 秒）而非 turn 计数，快速连续对话时可能过于频繁

> ⚠️ 定级调整：原 P1 → P2。核心触发机制已比较完善，剩余优化不影响主流程。

**相关代码**：`packages/core/src/memory/auto-memory.ts`、`apps/server/src/slash-commands.ts`

### P2: 记忆召回没有与对话上下文关联 ✅

**已实现**：在 `buildMessages()` 中，memory recall 的 query 从纯 `userMessage` 升级为 `enrichedQuery`。

**实现方式**：
- 新增 `buildEnrichedRecallQuery()` 辅助函数
- 融合策略：取最近 2 条 assistant 消息（各截断 200 字），拼接到 userMessage 后
- 总 query 长度上限 600 字，避免词法召回被长上下文淹没
- 不引入额外 LLM 调用，纯启发式截取——词法召回对关键词覆盖度要求高，精确度靠 RRF 融合兜底

**典型场景**：
- 用户："帮我改一下那个函数" + 上轮 assistant 提到 "parseFrontmatter"
- enrichedQuery = "帮我改一下那个函数 parseFrontmatter..."
- 召回能命中 parseFrontmatter 相关记忆

---

## 六、LLM Provider

### ✅ P1: LLMRouter 无健康检查

**修复方案**：新增 `CircuitBreaker` 类实现三态熔断器（closed → open → half-open）。每个 profile 连续 3 次失败后进入 open 状态（熔断 30 秒），冷却期过后自动转 half-open 允许试探请求，试探成功则恢复 closed。路由时跳过 open 状态的 profile，直接尝试下一个 fallback。

**相关代码**：`apps/server/src/llm-router.ts`

---

## 七、代码检索与索引

### P2: VectorStore 全量内存，大项目 OOM 风险

**现状**：所有向量都存在内存中，JSONL 持久化。万级文件的项目可能有数十万 chunk，向量维度 1536，占用 ~1GB 内存。

**建议**：
1. 增加磁盘索引模式（如 `hnswlib-node` 或 SQLite-vss）
2. 或按文件分片，LRU 淘汰冷 chunk 的向量

### P2: 增量索引的向量更新粒度粗

**现状**：文件变更时，`upsertFile` 会删除该文件所有旧向量，重新 embed 所有 chunk。

**问题**：小改动（改一行注释）也触发全文件重新 embed，浪费 API 调用。

**建议**：做 chunk 级 diff，只重新 embed 变更的 chunk。

### P3: BM25 中文分词简陋

**现状**：中文按单字分词（`[\u4e00-\u9fa5]`），没有词级分词。

**问题**：搜"数据库"时，BM25 会拆成"数""据""库"三个 token，召回噪声大。

**建议**：接入 `jieba` 或 `intl.Segmenter` 做中文分词。

---

## 八、前端与编辑器

### P1: Pending Edit accept 时缺少文件外部修改检测

**现状**：`accept()` 和 `acceptAll()` 写盘时没有检查文件在 propose 之后是否被外部修改过。

**问题**：
- 如果用户在 IDE 中手动编辑了文件，然后 accept AI 的 pending edit，可能覆盖用户的手动修改
- `propose()` 时读取的 `oldContent` 可能已经过时

**建议**：accept 前检查文件 mtime 或 content hash，如果文件已被修改，提示用户重新生成。

**相关代码**：`apps/server/src/pending-edit.ts` 第 99-109 行

### P2: Checkpoint 回滚没有 diff 预览

**现状**：`/api/checkpoints/restore` 直接回滚，用户看不到回滚会丢失哪些改动。

**建议**：增加 diff 预览 API 和前端 UI。

### P2: Ghost Text 补全没有上下文感知

**现状**：`/api/complete` 只用当前位置前后的代码做 FIM 补全，没有结合 Chat 上下文。

**建议**：将最近的 Chat 讨论主题作为 hint 注入补全 prompt。

---

## 九、安全与权限

### ✅ P1: run_command 危险命令黑名单 —— 已修复

**修复说明**：`exec-policy.ts` 已实现完善的三级分类体系：

- **DENY 黑名单**（约 30 个命令）：sudo, rm, chmod, chown, kill, dd, mkfs, ssh, eval, exec, source, brew, apt 等
- **AUTO 白名单**：ls, cat, head, tail, node, npm, pnpm, git (只读子命令) 等安全命令
- **危险参数模式检测**：即使程序在白名单中，也可能因危险参数被升级（如 `rm -rf /`、`curl|bash`、`base64 -d`、访问 `~/.ssh`）
- **Pipeline 拆分**：按 `|`、`&&`、`||`、`;`、`$()`、反引号拆分复合命令，每段独立判定
- **Permission Profile 融合**：sandbox/approval profile 可进一步收紧策略

**相关代码**：`apps/server/src/exec-policy.ts`、`apps/server/src/permission-aware-policy.ts`

> ⚠️ 注意：云端版 `sandbox-tools.ts` 中的 run_command 没有黑名单，完全信任沙箱隔离。这是合理的设计选择。

### P3: SSRF 防护仍有细微缺口

**现状**：`web-fetch-tool.ts` 已实现较完善的 SSRF 防护：

**已实现**：
- ✅ 协议白名单（只允许 http/https，禁止 file:// 等）
- ✅ DNS 解析后二次校验 IP（`ensureSafeDns` → `dns.lookup` → `ensureSafeUrl`）
- ✅ 重定向限制（最多 5 次，每次完整安全检查）
- ✅ IPv6 loopback 检查（`[::1]` 和 `::1`）
- ✅ 域名黑名单（localhost、.local、.internal 等）

**仍可优化（P3 级别）**：
- 未处理 IPv6 私有地址段（`fc00::/7`、`fe80::/10`）
- 未处理十进制/八进制 IP 表示（如 `http://2130706433/` 即 `127.0.0.1`）
- `dns.lookup()` 错误时放行（让 fetch 自行处理）
- 没有做端口限制

> ⚠️ 定级调整：原 P2 → P3。核心防护（协议限制、DNS 重绑定、重定向限制）已实现。

### P2: API 认证 Token 没有过期机制

**现状**：`X-Mini-Token` 是静态配置，没有过期和轮换机制。注意：`key-rotator.ts` 是另一套机制，不影响 X-Mini-Token。

**建议**：增加 JWT 或 TOTP 动态 token。

---

## 十、云版本 (server-cloud)

### P1: 多租户数据隔离不完整

**现状**：所有数据访问都通过 `PgStorage(req.userId)` 过滤，但：
- 沙箱文件系统隔离依赖 E2B，但 E2B 接入未完成
- Mem 沙箱模式所有用户共享内存 Map → 文件可能互相可见

**建议**：完成 E2E 沙箱接入，或至少在 Mem 沙箱中按 userId 隔离。

### P2: Token 计费精度不足

**现状**：只统计 `usedTokens`，没有区分 input/output/cache token，也没有按模型计费。

**建议**：
1. 从 LLM response 中解析 usage（`prompt_tokens`、`completion_tokens`、`cache_read_tokens`）
2. 按模型和 token 类型分别计费

---

## 十一、工程与构建

### P2: server-node 跨应用引用是反模式

**现状**：server-node 通过 `../../server/src/*.js` 相对路径引用 server 的代码。

**问题**：
- 修改 server 代码时可能不知道影响 server-node
- esbuild 能 resolve 但 IDE 无法跳转

**建议**：提取 `packages/server-core/` 包，server 和 server-node 共同依赖。

### P3: 没有端到端测试

**现状**：只有 unit test（vitest），没有 E2E 测试。

**建议**：至少增加 Chat 流程的 E2E 测试（Playwright / vitest + fetch mock）。

---

## 十二、上下文工程与 Token 经济

### P1: System 消息未分层缓存，Prompt Cache 命中率低

**现状**：`buildMessages()` 把 system prompt + autoContext + memory + systemExtras 全部拼在一个 system 消息里。任何一部分变化，整个 system 消息的 Prompt Cache 都会失效。

**问题**：
- Anthropic Prompt Cache 按前缀匹配，system 内容变化 → 全部缓存失效
- 每轮重复发送 ~2000 token 的稳定 system prompt + ~3000 token 的 tools schema

**建议**：
```typescript
// 拆成3段，分层缓存
// 对于支持多 system message 的 provider（OpenAI）：
{ role: 'system', content: systemBase, cacheHint: 'ephemeral' }       // 稳定，100%命中
{ role: 'system', content: rulesAndPermissions, cacheHint: 'ephemeral' } // 半稳定
{ role: 'system', content: dynamicContext }                              // 动态，不缓存
// 对于 Anthropic（只接受单个 system 参数）：
// 在 provider 适配层将多段拼回单条，但按稳定性排序确保稳定前缀不变
```

**注意**：Anthropic API 只支持一个 system 参数。拆分策略需要在 provider 适配层实现——将稳定部分放前面，动态部分放后面，利用 Anthropic 的前缀匹配 cache。

**相关代码**：`packages/core/src/context/builder.ts` 第 174-182 行

### P2: 工具 Schema 每轮全量发送，未做 Progressive Disclosure ✅

**已实现**：基于 AgentMode 的 Progressive Tool Disclosure。

**实现方式**：
- `ToolRegistry.CHAT_ONLY_PROFILE` 已定义（read_file, list_files, grep_search, find_symbol, search_web）
- `ToolRegistry.filter()` 方法创建子 registry（共享同一份 tool 实例）
- 在 `main.ts` 的 runAgent 调用处：
  - `ask` 模式 → 空 registry（无工具）
  - `plan` 模式 → `registry.filter(CHAT_ONLY_PROFILE)`（5 个只读工具，~600 token）
  - `agent` 模式 → 全量 registry（27+ 工具）

**节省**：plan 模式下工具 schema 从 ~3000 token 降至 ~600 token，每轮节省 ~2400 token。

**未实现（P3 远期）**：Agent 模式按任务阶段动态裁剪（探索阶段只给读工具，执行阶段给写工具）

### P2: 缺少 Token 预算分配策略

**现状**：没有显式的 token 预算分配，各路注入硬截断但不统筹。

**建议**：
| 区块 | 预算占比 | 说明 |
|------|---------|------|
| System Prompt | 5% | 固定成本 |
| AutoContext | 0-15% | 按检索质量动态分配 |
| History | 30-50% | 压缩后 |
| User Message | 5-10% | 用户输入 |
| Output | 20% | LLM 生成空间 |

### P3: autoContext 低质量时仍注入

**现状**：无论检索质量如何，autoContext 最多注入 6 条。如果检索结果与用户问题不相关，反而会干扰 LLM 注意力。

**建议**：增加相似度阈值——top-1 相似度 < 0.5 时不注入 autoContext，让 LLM 主动检索。

---

## 十三、异步架构

### ✅ P1: 同一 Session 无并发锁 —— 已修复

**修复方案**：新增 `SessionLock` 类（Promise 链式互斥），集成到 `SessionStore.lock`。在 `/api/chat` handler 中，对持久化 session 的请求在 turn 开始前 acquire 锁，在 finally 块中 release。

**相关代码**：`apps/server/src/session-store.ts`、`apps/server/src/main.ts`

### P2: 子 Agent 结果延迟推送

**现状**：子 Agent 完成后注入消息到父 session，但父 Agent 只能在下一轮 turn 看到结果。

**问题**：
- 父 Agent 可能在等待子 Agent 结果时做无用功
- 无法实现"父 Agent 实时监控子 Agent 进度"

**建议**：
1. 短期：在父 Agent 的 loop 中增加"检查子 Agent 状态"的检查点
2. 长期：引入 Event Bus + WebSocket 实时推送

---

## 新增问题

### ✅ P1: run_command 执行结果不归并到 PendingEdit 体系

**修复方案**：在 `ToolContext` 中新增 `checkpoint` 钩子。`run_command` 执行前通过 `detectAffectedFiles()` 启发式检测文件修改类命令（cp/mv/rm/touch/mkdir/sed -i/npm install 等），自动创建 checkpoint 快照目标文件。用户可通过 checkpoint revert 撤销 run_command 的副作用。

**相关代码**：`packages/core/src/agent/builtin-tools.ts`、`packages/core/src/agent/tool-registry.ts`、`apps/server/src/main.ts`

### P2: acceptAll 两阶段提交依赖 fs.rename 跨设备原子性

**现状**：`acceptAll()` 的 Phase 2 使用 `fs.rename` 将 staging 文件搬到目标路径。`fs.rename` 在同一设备上是原子操作，但如果 `.minicodeide/staging/` 和目标文件不在同一磁盘/分区上，`rename` 会失败（EXDEV 错误）。

**问题**：
- 符号链接场景下，staging 和目标可能在不同的文件系统上
- Docker volume mount 场景下也可能跨设备

**建议**：增加 EXDEV 错误的 fallback——`rename` 失败时改用 `copyFile` + `unlink`。

### ✅ P2: subagentProgressBuffer 无上限可能导致客户端 UI 刷新风暴

**现状修正**：代码中已有 200 条上限保护（`main.ts` 第 374-376 行），超过后丢弃最旧的条目。

**相关代码**：`apps/server/src/main.ts` 第 373-377 行

---

## 十四、历史会话列表模块（深度排查）

> 本节是对历史会话模块的完整逻辑审计，覆盖前端三端（Desktop IDE / Agents Window / Web）、后端双存储（SessionStore-JSONL / PgStorage-Postgres）、以及跨模块数据一致性问题。

### 架构总览

**前端三入口**：

| 入口 | 根组件 | Store | 会话列表组件 |
|------|--------|-------|-------------|
| Desktop 主 IDE | `<App>` | `useStore` (store.ts) | SessionsDrawer |
| Desktop Agents Window | `<AgentsApp>` | `useAgentsStore` (agents-window/store.ts) | AgentsSidebar |
| Web App | `<App>` → `<AgentsApp>` | `useAgentsStore` (复用 desktop) | AgentsSidebar |

**后端双存储**：

| 存储层 | 实现类 | 数据位置 | 并发锁 |
|--------|--------|----------|--------|
| Desktop 本地 | `SessionStore` (server-core) + `JsonlStorage` (storage) | `<workspace>/.minicodeide/sessions/<id>.jsonl` | SessionLock (仅 SessionStore) |
| Cloud 云端 | `PgStorage` (storage) | Postgres DB via Prisma | 无 |

**路由决策**：前端 `sessionApiBase()` 根据 `authUser` 状态决定走云端（`/cloud-api`）还是本地 server（`''`）。

**两层 Mode 体系**：

项目中存在**两套并行但语义不同的 mode 系统**，前端通过一层映射连接：

```
前端 UI 层（AgentMode）           后端存储层（SessionMode）
  agent（全量工具，多步执行）   →    code
  ask  （无工具，单轮问答）     →    work
  plan （只读工具，规划模式）   →    code  ← 当前代码错误映射为 'plan'
```

| 概念 | 值域 | 定义位置 | 作用 |
|------|------|----------|------|
| **SessionMode** | `'work' \| 'code'` | `storage/types.ts:10`, `server-core/session-store.ts:29` | 会话分类和过滤（存储层） |
| **AgentMode** | `'ask' \| 'agent' \| 'plan'` | `store.ts:154`, `agents-window/store.ts:18`, `core/prompts:31` | AI 行为差异（工具集、system prompt、执行步数） |

**设计意图**：
- 主 IDE 的 SessionsDrawer **不按 mode 过滤**，显示所有会话（因为它面向代码编辑场景，大部分是 code）
- Agents Window 是**多模式独立窗口**，有 ModeToggle（智能体/Ask/Plan）切换 UI，切 mode 时重新拉取对应会话列表
- 主 IDE 的 `/api/chat` 直接传 AgentMode（server-node 的 chat handler 期望 AgentMode）
- Agents Window 的 `/api/chat` 映射为 SessionMode 后传（server-cloud 的 chat handler 期望 SessionMode）
- 三种 AgentMode 的行为差异：`agent`=全量工具+多步循环，`ask`=无工具+单轮，`plan`=只读工具+单轮+PlanMode 专属 system prompt

---

### P0: agents-window 的 plan 模式映射错误 — 导致 plan 会话列表永远为空

**问题描述**：agents-window 切换到 Plan 模式后，会话列表为空，新建的 plan 会话也找不到。

**根因分析 — `plan` 映射到了不存在的 SessionMode 值**：

agents-window/store.ts 在 `loadSessions` 和 `createSession` 中做了同一套映射：
```typescript
mode === 'agent' ? 'code' : mode === 'ask' ? 'work' : 'plan'
//                                                              ^^^^^^
//                                                     plan 不是合法的 SessionMode！
```

SessionMode 的类型定义只有 `'work' | 'code'`，`'plan'` 不在其中。但因为是字符串传递，运行时不会报错。完整链路：

1. **createSession** 传 `mode: 'plan'` → server-node POST handler（register.ts 第 871 行）只认 `'work'` 和 `'code'`：
   ```typescript
   const mode = body?.mode === 'work' ? 'work' : body?.mode === 'code' ? 'code' : undefined;
   // body.mode === 'plan' → 既不是 'work' 也不是 'code' → undefined → 兜底 'code'
   ```
   session 实际被存为 `mode='code'`。

2. **loadSessions** 请求 `GET /api/sessions?mode=plan` → 后端过滤 `(x.mode ?? 'code') === 'plan'` → 永远匹配不到（实际存的是 `code`）→ **列表永远为空**。

3. **PgStorage 也一样**：`create()` 的 `mode: opts.mode ?? 'work'`，但 `'plan'` 不是 undefined，所以直接存入 DB 为 `mode='plan'`。然后 `list()` 用 `where.mode = 'plan'` 查询 → 能查到。**但 DB 里存了一个不在类型定义中的值，这是脏数据**。

**修复方案**：
1. agents-window/store.ts 的映射修正为 `agent→code`, `ask→work`, `plan→code`（plan 也是代码探索场景）
2. AgentsMain.tsx 第 320 行同步修正
3. 清理已有脏数据：DB 中 `mode='plan'` 的记录迁移为 `mode='code'`
4. DB schema 的 comment 补充说明映射关系

**相关文件**：
- `apps/desktop/src/agents-window/store.ts` 第 164、181 行
- `apps/desktop/src/agents-window/AgentsMain.tsx` 第 320 行
- `apps/server-node/src/handlers/register.ts` 第 871 行
- `packages/storage/src/types.ts` 第 10 行（SessionMode 定义）

---

### P0: 历史会话列表在新建对话后未正确更新（云端场景）

**问题描述**：用户新建对话并输入问题后，打开历史记录面板，列表未显示最新的会话。

**根因分析 — 主 IDE createSession 不传 mode，云端默认值与本地不同**：

主 IDE 的 `createSession(title)` 只传 title 不传 mode（store.ts 第 555-569 行）：
```typescript
async createSession(title) {
  const r = await sessionFetch('/api/sessions', {
    method: 'POST',
    body: JSON.stringify({ title }),  // ⚠️ 不传 mode
  });
}
```

此时后端用各自的默认值兜底：
- **本地** SessionStore/JsonlStorage 默认 `'code'` → session 存为 code
- **云端** PgStorage 默认 `'work'`（prisma-pg.ts 第 126 行），DB schema 也默认 `'work'`（schema.prisma 第 49 行）→ session 存为 work

如果用户登录后（走云端）从主 IDE 新建会话，session 被存为 `mode='work'`。当 agents-window 切到"智能体"（agent→code）模式查询时，这个会话不会出现在列表中。

**注意**：主 IDE 的 SessionsDrawer 不过滤 mode（拉全量），所以在主 IDE 内部这个问题不会暴露。但 agents-window 按 mode 过滤时就会不一致。

**修复方案**：
1. 主 IDE `createSession` 增加 mode 映射逻辑，传 `mode: 'code'`（主 IDE 面向代码编辑，默认 code 即可）
2. PgStorage 的默认值改为 `'code'`（与本地对齐），或至少保持与调用方一致
3. `POST /api/sessions` 云端路由增加 workspaceRoot 的默认值回退（与 desktop 对齐）

**相关文件**：
- `apps/desktop/src/store.ts` 第 555-569 行
- `packages/storage/src/prisma-pg.ts` 第 126 行
- `packages/storage/prisma/schema.prisma` 第 49 行

---

### P0: 登录态下 title 和 messageCount 显示不正确

**问题描述**：登录态（云端 PgStorage）的历史列表不能正确展示 title 和消息条数。

**根因分析**：

**title 问题**：

1. `PgStorage.append()` 的 title 更新 SQL 只检查 `'New chat'`，不检查 `'Untitled'`：
   ```typescript
   // prisma-pg.ts 第 177-181 行
   WHERE id = ${id} AND title = 'New chat'
   ```
   但 `list()` 方法（第 77 行）同时检查 `'New chat'` 和 `'Untitled'`。如果某个会话 title 为 `'Untitled'`（如迁移或边界场景），append 时永远不会自动更新 title。

2. `append()` 中 title 更新和 message 插入不在同一事务中（第 149-182 行），存在竞态：message 已创建、messageCount 已增 1，但 title 尚未更新。高并发场景下列表可能看到 `messageCount=1` 但 `title='New chat'` 的中间态。

3. title 更新 SQL 在**每条** user 消息时都执行（不仅仅是第一条），虽然 `WHERE title = 'New chat'` 保证不会覆盖，但产生不必要的 DB 写入。

4. `list()` 中推断 title 使用 `DISTINCT` + `ORDER BY ts ASC` 取首条 user 消息（第 82-92 行），在某些 Postgres 版本下可能不稳定，应改用 `DISTINCT ON`。

**messageCount 问题**：

`messageCount` 统计了**所有角色的消息**（user + assistant + tool + system）。一次 agent 对话可能产生 10+ 条 tool 消息。

```typescript
// SessionStore.list() 第 149 行
const messageCount = s.messages.length  // 包含所有 role

// PgStorage.list() 第 40 行
row._count?.messages ?? row.messageCount  // 同样包含所有 role
```

用户期望看到的是"对话轮数"（user + assistant 对数），而非包含工具调用的总消息数。

**修复方案**：
1. `append()` 的 title 更新 SQL 条件改为 `WHERE title IN ('New chat', 'Untitled')`
2. 将 title 更新纳入 message 创建的同一事务
3. `messageCount` 改为只统计 user + assistant 消息（或新增 `turnCount` 字段）
4. `list()` 的 title 推断查询改用 `DISTINCT ON`
5. title 更新增加内存缓存检查，避免每条 user 消息都写 DB

**相关文件**：
- `packages/storage/src/prisma-pg.ts` 第 66-98、149-182 行
- `packages/server-core/src/store/session-store.ts` 第 145-161、217-220 行
- `apps/desktop/src/components/SessionsDrawer.tsx` 第 93-96 行

---

### P0: 点击历史会话回显不正确 — tool_call 消息未持久化

**问题描述**：点击历史会话后，工具调用过程展示不完整，工具链卡片缺少标题、参数等信息。

**根因分析**：

服务端在 `/api/chat` 的 SSE 流中，**只存 tool_result，不存 tool_call**：

```typescript
// server-node register.ts 第 1438-1457 行
if (ev.type === 'tool_call' && persistSession && currentTurnId) {
  s.sessions.append(rawSessionId!, {
    role: 'tool',
    content: String(result).slice(0, 2000),  // 只存 result 内容
    toolName,
    uiMeta: { _toolRole: 'result', _toolName: toolName, _toolArgs: ... },
  });
}
```

tool_call 本身的前端渲染消息（`_toolRole: 'call'`）从未被存入 session。

**云端更严重**：cloud `chat.ts` 第 143-144 行只通过 SSE 发送 tool 事件，**完全没有调用 `storage.append()` 持久化工具消息**。这意味着云端用户的回显中**所有工具调用过程都会丢失**。

**影响**：回显时 ChatPanel 的 `ThinkingProcess` / `ToolChainCard` 组件做 call-result 配对时找不到匹配的 call，导致工具链卡片信息缺失。

**修复方案**：
1. server-node: 在 `tool_call` 事件处理时同时存一条 `_toolRole: 'call'` 的消息（含 toolName、toolArgs）
2. server-cloud: 在 SSE 推送 tool 事件的同时调用 `storage.append()` 持久化 tool_call 和 tool_result 两条消息
3. 统一两端（server-node / server-cloud）的 tool 消息持久化逻辑

**相关文件**：
- `apps/server-node/src/handlers/register.ts` 第 1438-1457 行
- `apps/server-cloud/src/routes/chat.ts` 第 143-144 行
- `apps/desktop/src/store.ts` 第 571-596 行（switchSession 回显逻辑）
- `apps/desktop/src/agents-window/AgentsMain.tsx` 第 127-153 行

---

### P1: PgStorage 写操作缺少 userId 隔离 — 多租户安全隐患

**问题描述**：PgStorage 的读操作（get/list）正确过滤了 userId，但写操作没有。

**具体表现**：

以下写操作均使用 `where: { id }` 而**不包含 userId**：
- `append()` 第 151-182 行：`session.update({ where: { id } })`
- `rename()` 第 186 行：`session.update({ where: { id } })`
- `startTurn()` 第 201-218 行
- `appendChunk()` 第 225-239 行：Raw SQL `WHERE id = ${id}`
- `appendTool()` 第 242-249 行
- `endTurn()` 第 252-275 行
- `interruptTurn()` 第 277-282 行

只有 `delete()` 第 195 行正确使用了 `{ id, userId: this.userId }`。

**影响**：如果用户 A 知道用户 B 的 session ID，可以修改 B 的会话数据（追加消息、修改标题等）。

**修复方案**：所有写操作的 where 条件统一加入 `userId: this.userId`，或在 SQL 中 JOIN Session 表校验 userId。

**相关文件**：`packages/storage/src/prisma-pg.ts` 全文

---

### P1: 云端 PgStorage 无并发写保护

**问题描述**：Desktop 的 `SessionStore` 有 `SessionLock`（Promise 链式互斥），但 `PgStorage` 没有任何并发保护。

**影响**：如果用户快速双击发送（或两个请求并发命中同一 session），turn 生命周期会交错，导致：
- `interruptedTurn` 状态损坏
- 不同 turn 的 chunks 混合
- `endTurn()` 可能结束错误的 turn

**修复方案**：在 `PgStorage` 中引入类似的 `SessionLock`，或在 cloud `chat.ts` 的 handler 中加 session 级锁。

**相关文件**：
- `packages/storage/src/prisma-pg.ts`
- `apps/server-cloud/src/routes/chat.ts`
- `packages/server-core/src/store/session-store.ts` 第 83-106 行（SessionLock 参考实现）

---

### P1: Web App 的 authUser 状态未设置，sessionFetch 路由逻辑脆弱

**问题描述**：Web App 启动时通过 `fetch('/api/auth/me')` + `POST '/api/auth/anonymous'` 完成鉴权，但**从未调用 `useStore.getState().checkAuth()`**。

```typescript
// web/src/App.tsx 第 24-36 行
const me = await fetch('/api/auth/me', { credentials: 'include' });
if (!me.ok) {
  await fetch('/api/auth/anonymous', { method: 'POST', credentials: 'include' });
}
setReady(true);  // ⚠️ 没有调用 checkAuth() 设置 authUser
```

`sessionApiBase()` 检查 `authUser`，但它始终为 `null`：
```typescript
function sessionApiBase(): string {
  const user = useStore.getState().authUser;  // 始终为 null
  if (user && !user.isAnonymous) {
    return CLOUD_API || '/cloud-api';
  }
  return '';  // ← Web App 永远走这里
}
```

**当前不影响**：web app 部署在同源 cloud server 上，空 base 即同源请求。但跨域部署时所有 API 调用会失败。

**修复方案**：Web App 初始化时调用 `checkAuth()` 正确设置 authUser 状态。

**相关文件**：
- `apps/web/src/App.tsx` 第 24-36 行
- `apps/desktop/src/store.ts` 第 22-44 行

---

### P1: 主 IDE 与 Agents Window 的 Store 完全独立，无实时同步

**问题描述**：Desktop 主 IDE 使用 `useStore`，Agents Window 使用独立的 `useAgentsStore`，两个 zustand store 之间没有事件同步机制。

**影响**：
- 在 Agents Window 中新建会话，主 IDE 的 SessionsDrawer 不会自动刷新
- 在主 IDE 中删除会话，Agents Window 的 sidebar 不会更新
- 两个窗口可能同时操作同一 session，产生数据不一致

**修复方案**：
1. 短期：通过 `BroadcastChannel` 或 `window.addEventListener('storage')` 实现跨窗口通知
2. 长期：合并为一个 store，或抽取 SessionService 层统一管理

**相关文件**：
- `apps/desktop/src/store.ts`
- `apps/desktop/src/agents-window/store.ts`

---

### P1: PgStorage.interruptTurn() 不清理 Session.interruptedTurn 字段

**问题描述**：`interruptTurn()` 只更新 Turn 表，不清理 Session 表的 `interruptedTurn` JSON 字段。

```typescript
// prisma-pg.ts 第 277-282 行
async interruptTurn(_id: string, turnId: string, reason?: string) {
  await this.prisma.turn.update({
    where: { id: turnId },
    data: { status: 'interrupted', endedAt: BigInt(Date.now()), finalText: reason ?? null },
  }).catch(() => undefined);
  // ⚠️ 没有清理 Session.interruptedTurn
}
```

如果 `interruptTurn()` 被调用但 `endTurn()` 未被调用（如进程崩溃），`Session.interruptedTurn` 将永远残留，每次回显该会话时都会显示"继续？"的恢复提示。

**修复方案**：`interruptTurn()` 中同步清理 `Session.interruptedTurn = null`。

**相关文件**：`packages/storage/src/prisma-pg.ts` 第 277-282 行

---

### P2: 两个 SessionStore 实现（SessionStore / JsonlStorage）功能重复但不一致

**问题描述**：`packages/server-core/src/store/session-store.ts` 的 `SessionStore` 和 `packages/storage/src/jsonl.ts` 的 `JsonlStorage` 是两套几乎相同功能的实现，但维护分散。

**关键差异**：
- `SessionStore` 有 `SessionLock` 并发保护，`JsonlStorage` 没有
- `SessionStore.list()` 是同步方法，`JsonlStorage.list()` 是异步方法（接口不兼容）
- `SessionStore` 被 server-node 使用，`JsonlStorage` 被 `createStorage()` 工厂使用
- `createStorage({ kind: 'postgres' })` 仍然抛出 "not implemented"（factory.ts 第 31-32 行），尽管 `PgStorage` 已完整实现

**修复方案**：
1. 统一接口：让 `SessionStore` 实现 `SessionStorage` 接口
2. `JsonlStorage` 增加 `SessionLock`
3. `createStorage()` 工厂支持 `'postgres'`
4. 长期合并为一个实现

**相关文件**：
- `packages/server-core/src/store/session-store.ts`
- `packages/storage/src/jsonl.ts`
- `packages/storage/src/types.ts`
- `packages/storage/src/factory.ts` 第 31-32 行

---

### P2: workspaceRoot 路径比较使用严格字符串匹配

**问题描述**：`loadSessions` 的 workspaceRoot 过滤使用严格字符串比较。

```typescript
// agents-window/store.ts 第 140-156 行
// jsonl.ts 第 67 行
list.filter((x) => x.workspaceRoot === ws)
```

macOS 下路径可能因尾斜杠（`/foo` vs `/foo/`）、大小写（HFS+ 不区分大小写）、符号链接（`/var` → `/private/var`）等不一致，导致会话列表为空。

**修复方案**：比较前统一 `path.resolve()` 规范化路径。

**相关文件**：
- `apps/desktop/src/agents-window/store.ts` 第 140-156 行
- `packages/storage/src/jsonl.ts` 第 67 行

---

### P2: 登录/登出期间 sessionFetch 路由状态不稳定

**问题描述**：`sessionFetch` 在调用时根据 `authUser` 的**当前状态**决定路由。如果用户在会话期间登录或登出：

```typescript
// store.ts 第 30-44 行
function sessionFetch(path, init) {
  const base = sessionApiBase();  // 取决于 authUser 的瞬时状态
  // ...
}
```

- 已创建的本地 session 在登录后不会自动迁移到云端
- 登出后云端 session 无法在本地找到
- 正在进行的 SSE 流可能因后端切换而中断

**修复方案**：登录/登出时增加 session 迁移逻辑，或至少在 UI 上提示用户。

**相关文件**：`apps/desktop/src/store.ts` 第 22-44、769-794 行

---

### P2: createStorage() 工厂不支持 'postgres' 类型

**问题描述**：尽管 `PgStorage` 已完整实现并导出，工厂函数仍然抛出异常。

```typescript
// factory.ts 第 31-32 行
case 'postgres':
  throw new Error('PrismaPgStorage not implemented yet (M2)');
```

云端 server 通过直接 `new PgStorage()` 绕过了工厂，但 `STORAGE_KIND=postgres` 环境变量路径不可用。

**修复方案**：工厂中实现 `postgres` case，直接返回 `new PgStorage(prisma, userId)`。

**相关文件**：`packages/storage/src/factory.ts` 第 31-32 行

---

### P2: AgentsMain 历史消息加载的 ChatMsg 类型映射与主 IDE 不一致

**问题描述**：Agents Window 的 ChatMsg 接口中工具字段是 `toolName`（无下划线前缀），主 IDE 是 `_toolName`。

```typescript
// AgentsMain.tsx 第 137-146 行
const ms: ChatMsg[] = (j.messages ?? []).map((m: any) => ({
  role: m.role, content: m.content, ts: m.ts,
  ...(m.toolName ? { toolName: m.toolName } : {}),  // ← toolName
  ...(m.uiMeta ?? {}),  // uiMeta 展开会覆盖为 _toolName
}));
```

虽然 `uiMeta` 展开能覆盖正确字段名，但 `m.toolName` 的单独映射是冗余且不一致的。

**修复方案**：统一 ChatMsg 接口定义，去除冗余映射。

**相关文件**：`apps/desktop/src/agents-window/AgentsMain.tsx` 第 137-146 行

---

### P2: 云端 session create 缺少 workspaceRoot 默认值回退

**问题描述**：Desktop 创建 code 模式会话时有 workspaceRoot 回退到当前 workspace，云端没有。

```typescript
// Desktop register.ts 第 871-873 行
const workspaceRoot = mode === 'code'
  ? (body?.workspaceRoot ?? s.workspace)  // ← 有回退
  : body?.workspaceRoot;

// Cloud sessions.ts 第 29-36 行
return storage(req).create({
  workspaceRoot: body.workspaceRoot,  // ← 无回退
});
```

**修复方案**：云端路由中增加 workspaceRoot 的默认值处理。

**相关文件**：`apps/server-cloud/src/routes/sessions.ts` 第 29-36 行

---

## 十五、其他新发现的逻辑不自洽

### P1: 云端 chat 路由不持久化 tool 消息到存储层

**问题描述**：cloud `chat.ts` 中，工具调用事件仅通过 SSE 推送到前端，未调用 `storage.append()` 写入数据库。这是十四中 P0 问题的云端特例，单独列出以强调其严重性。

**影响**：云端用户的所有历史会话回显中，工具调用过程完全丢失。

**修复方案**：在 cloud `chat.ts` 的 tool_call 事件处理中，增加 `storage.append()` 调用，同时存 tool_call 和 tool_result 两条消息。

**相关文件**：`apps/server-cloud/src/routes/chat.ts` 第 143-144 行

---

### P2: Agents Window 缺少 interruptedTurn 恢复能力

**问题描述**：`useAgentsStore` 的 session 类型定义（第 50-59 行）不包含 `interruptedTurn` 字段。即使后端正确返回了 `interruptedTurn` 信息，Agents Window 的 sidebar 和 main 区域也不会显示"继续？"的恢复按钮。

**修复方案**：agents-window store 的 SessionMeta 类型中增加 `interruptedTurn` 字段，AgentsMain 中增加恢复逻辑。

**相关文件**：`apps/desktop/src/agents-window/store.ts` 第 50-59 行

---

### P2: Agents Window toolEvents 不从历史消息恢复

**问题描述**：`toolEvents` 数组仅在实时 SSE 流中填充（流式渲染工具调用卡片），加载历史会话时不会从存储的消息中重建 toolEvents。

**影响**：历史会话回显时，工具调用的流式渲染效果（如 ThinkingProcess 动画）不会出现。

**修复方案**：在 `AgentsMain.tsx` 的 useEffect 加载历史消息后，从 tool 类型的消息中重建 toolEvents 数组。

**相关文件**：`apps/desktop/src/agents-window/AgentsMain.tsx` 第 127-153 行

---

## 修复优先级计划（更新版）

### Mode 架构重构决策

> **核心结论**：参考 Cursor、Claude Code、Windsurf 等竞品的设计模式，mode 应该只影响**当次对话的 AI 行为**（工具集、system prompt、执行步数），**不应该作为会话的分类维度**。历史对话列表应统一展示所有会话，不按 mode 过滤。

**重构要点**：
1. agents-window 的 `loadSessions` **去掉 mode 过滤**，拉全量会话列表
2. ModeToggle（智能体/Ask/Plan）仅影响新建会话的默认 mode 和 composer 当前行为
3. plan 在存储层映射为 `code`（plan 也是代码探索场景），不再使用不存在的 `'plan'` SessionMode
4. 主 IDE 的 `createSession` 增加 `mode: 'code'` 参数
5. 用户可以在同一个会话中先 plan 规划、再 agent 执行（类似 Claude Code 的 Shift+Tab 体验）

---

### 第一批（P0 — 必须立即修复，预计 5 天）

| 序号 | 问题 | 涉及文件 | 状态 |
|------|------|----------|------|
| 1 | agents-window loadSessions 去掉 mode 过滤 + plan→code 映射修正 | agents-window/store.ts, AgentsMain.tsx | ✅ 已修复 |
| 2 | 主 IDE createSession 传 mode='code' | store.ts | ✅ 已修复 |
| 3 | PgStorage 默认 mode 改为 'code' + schema 同步 | prisma-pg.ts, schema.prisma | ✅ 已修复 |
| 4 | PgStorage append title 条件修复（加 'Untitled'） | prisma-pg.ts | ✅ 已修复 |
| 5 | messageCount 改为只统计 user+assistant | prisma-pg.ts, session-store.ts | ✅ 已修复 |
| 6 | server-node 持久化 tool_call 消息（补全 call-result 配对） | register.ts | ✅ 已修复 |
| 7 | server-cloud 持久化 tool_call + tool_result 消息 | chat.ts | ✅ 已修复 |
| 8 | PgStorage.list title 推断改用 DISTINCT ON | prisma-pg.ts | ✅ 已修复 |

### 第二批（P1 — 影响体验和安全，预计 5 天）

| 序号 | 问题 | 涉及文件 | 预估 |
|------|------|----------|------|
| 8 | PgStorage 所有写操作加 userId 过滤 | prisma-pg.ts | 1.5 天 |
| 9 | 云端 PgStorage 增加 SessionLock 或 handler 级锁 | prisma-pg.ts / chat.ts | 1.5 天 |
| 10 | Web App 初始化时调用 checkAuth() | web/App.tsx | 0.5 天 |
| 11 | 主 IDE 与 Agents Window store 同步机制 | store.ts, agents-window/store.ts | 1 天 |
| 12 | PgStorage.interruptTurn 清理 Session.interruptedTurn | prisma-pg.ts | 0.5 天 |

### 第三批（P2 — 体验优化，预计 5 天）

| 序号 | 问题 | 涉及文件 | 预估 |
|------|------|----------|------|
| 13 | 合并/统一 SessionStore 和 JsonlStorage | server-core, storage | 2 天 |
| 14 | workspaceRoot 路径比较规范化 | agents-window/store.ts, jsonl.ts | 0.5 天 |
| 15 | 登录/登出时 session 迁移提示 | store.ts | 1 天 |
| 16 | createStorage 工厂支持 postgres | factory.ts | 0.5 天 |
| 17 | ChatMsg 类型映射统一 | AgentsMain.tsx | 0.5 天 |
| 18 | 云端 session create 增加 workspaceRoot 回退 | sessions.ts | 0.5 天 |
| 19 | Agents Window 增加 interruptedTurn 恢复 | agents-window/store.ts, AgentsMain.tsx | 0.5 天 |
| 20 | Agents Window toolEvents 历史恢复 | AgentsMain.tsx | 0.5 天 |

### 全量修复优先级总表

| 优先级 | 项目 | 状态 | 预估工作量 |
|--------|------|------|-----------|
| ~~P1~~ | ~~MCP Tool 参数 Schema 注入~~ | ✅ 已修复 | - |
| ~~P1~~ | ~~Session 并发锁~~ | ✅ 已修复 | - |
| ~~P1~~ | ~~System 消息分层缓存~~ | ✅ 已修复 | - |
| ~~P1~~ | ~~Pending Edit accept 时外部修改检测~~ | ✅ 已修复 | - |
| ~~P2~~ | ~~acceptAll 跨设备 rename fallback~~ | ✅ 已修复 | - |
| ~~P1~~ | ~~子 Agent 结果推送 + messages 注入~~ | ✅ 已修复 | - |
| ~~P1~~ | ~~LLMRouter 健康检查 (Circuit Breaker)~~ | ✅ 已修复 | - |
| ~~P2~~ | ~~MCP 进程断连自动重连~~ | ✅ 已修复 | - |
| ~~P2~~ | ~~subagentProgressBuffer 限流~~ | ✅ 已有 200 条上限 | - |
| ~~P1~~ | ~~run_command 结果归并到 PendingEdit~~ | ✅ 已修复 (checkpoint) | - |
| ~~P1~~ | ~~Hard Compact 决策链保留~~ | ✅ 已修复 | - |
| ~~P1~~ | ~~Skill 自动触发 + keyword 匹配~~ | ✅ 已修复 | - |
| ~~P1~~ | ~~run_command 危险命令黑名单~~ | ✅ 已修复 | - |
| ~~P1~~ | ~~Auto-Memory `/remember` 命令~~ | ✅ 已修复 | - |
| ~~P2~~ | ~~Skill 热更新~~ | ✅ 已修复 | - |
| ~~P2~~ | ~~子 Agent 流式输出完善~~ | ✅ 已修复 | - |
| ~~P2~~ | ~~记忆召回上下文增强~~ | ✅ 已修复 | - |
| ~~P2~~ | ~~Progressive Tool Disclosure~~ | ✅ 已修复 | - |
| ~~P2~~ | ~~SSRF 防护~~ | ✅ 核心已实现（降为 P3） | - |
| **P0** | **agents-window plan 模式映射错误（列表永远为空）** | ✅ 已修复 | - |
| **P0** | **历史会话列表新建后未更新（云端 mode 默认值不一致）** | ✅ 已修复 | - |
| **P0** | **登录态 title 和 messageCount 不正确** | ✅ 已修复 | - |
| **P0** | **tool_call 消息未持久化（回显不完整）** | ✅ 已修复 | - |
| **P0** | **Mode 架构重构（历史列表不按 mode 过滤）** | ✅ 已修复 | - |
| P1 | 多租户数据隔离（PgStorage 写操作加 userId） | 待修复 | 1.5 天 |
| P1 | 云端 PgStorage 并发写保护 | 待修复 | 1.5 天 |
| P1 | Web App authUser 状态未设置 | 待修复 | 0.5 天 |
| P1 | 主 IDE 与 Agents Window store 同步 | 待修复 | 1 天 |
| P1 | PgStorage.interruptTurn 不清理 interruptedTurn | 待修复 | 0.5 天 |
| P2 | Auto-Memory 触发条件优化 | 降级为 P2 | 1 天 |
| P2 | MCP 资源协议实现 | ❌ 跳过（ROI 低） | - |
| P2 | Token 估算精度提升 | 待修复 | 1 天 |
| P2 | 向量索引磁盘化 | 待修复 | 3 天 |
| P2 | Checkpoint diff 预览 | 待修复 | 1 天 |
| P2 | Token 预算分配策略 | 待修复 | 2 天 |
| P2 | API 认证 Token 过期机制 | 待修复 | 1 天 |
| P2 | 合并 SessionStore / JsonlStorage | 待修复 | 2 天 |
| P2 | workspaceRoot 路径规范化 | 待修复 | 0.5 天 |
| P2 | 登录登出 session 迁移 | 待修复 | 1 天 |
| P2 | createStorage 工厂支持 postgres | 待修复 | 0.5 天 |
| P2 | ChatMsg 类型映射统一 | 待修复 | 0.5 天 |
| P2 | 云端 session workspaceRoot 回退 | 待修复 | 0.5 天 |
| P2 | Agents Window interruptedTurn 恢复 | 待修复 | 0.5 天 |
| P2 | Agents Window toolEvents 历史恢复 | 待修复 | 0.5 天 |
| P3 | Prompt Cache 命中统计 | 待修复 | 0.5 天 |
| P3 | BM25 中文分词 | 待修复 | 1 天 |
| P3 | server-core 包提取 | 待修复 | 2 天 |
| P3 | E2E 测试 | 待修复 | 3 天 |
| P3 | autoContext 质量阈值 | 待修复 | 0.5 天 |
| P3 | SSRF 细微缺口（IPv6 私有段等） | 待修复 | 0.5 天 |

**剩余预估：~35 人天**（其中历史会话模块 ~16 天，已累计修复 ~14 人天工作量）
